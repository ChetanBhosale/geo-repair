import type { FixSiteInput, FixSiteResult } from "../../shared";

// Activities for the fix-site queue. Logic added later.
export async function fixSite(_input: FixSiteInput): Promise<FixSiteResult> {
  throw new Error("fixSite activity not implemented yet");
}
